# HENTR BODEN LAPTOP — $HENTR

Статический одностраничник, без сборки. Всё в одном `index.html`.

Контракт и ссылки меняются в блоке `SETTINGS` в первых строках файла:
`window.CONTRACT`, `window.TWITTER`, `window.TELEGRAM`, `window.BUY_URL`.

Деплой: Vercel → Import → Framework: Other, build command пустой.
